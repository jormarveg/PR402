package com.jorge.pr402.vehicles

/**
 * Clase que representa un coche, hereda de la clase [Vehicle].
 */
class Car(wheels: Int, engine: String, seats: Int, color:String, model: String) :
    Vehicle(wheels, engine, seats, color, model)